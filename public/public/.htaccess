RewriteEngine On

# JWT Authorization header passthrough to PHP
SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1

# Front controller
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
