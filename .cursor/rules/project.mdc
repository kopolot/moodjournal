---
description: MoodDic API — project status and invariants
alwaysApply: true
---

# MoodDic / MoodJournal API

Prototype Symfony 8.1 JSON API + Docker. Companion app: `moodjournalmobile` on GitHub.

- Mood domain lives under `/mood` (check-ins, XP, streaks). Prefer extending that over inventing parallel APIs.
- Stack runs via Docker Compose; only publish Apache on host port **8080**.
- Symfony root is `public/`; HTTP docroot is `public/public/`.
- Nested `mobile/` is a separate git repo — do not commit it here.
- No project-scoped MCP is required for this codebase.
